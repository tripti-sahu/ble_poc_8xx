const { BaseScreen } = loadLibrary("../screens/baseScreen");

class UtilsMethod extends BaseScreen {
    constructor(mobile, controller, gstConfig) {
        super(mobile, controller);
        this.gstConfig = gstConfig;
    }

    sendSG() {
        // TODO
    }

    fetchGraphMetadata() {
        // TODO
    }
}
exports.UtilsMethod = UtilsMethod;
